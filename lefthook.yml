pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx,json,css}"
      run: pnpm exec biome check --staged {staged_files}
      stage_fixed: true

pre-push:
  commands:
    typecheck:
      run: pnpm run typecheck
    test:
      run: pnpm run test
